public class Solution {
    public char FindTheDifference(string s, string t) {
        char result = '\0';

        foreach (var c in s) 
            result ^= c;
        foreach (var c in t) 
            result ^= c;

        return result;
    }
}