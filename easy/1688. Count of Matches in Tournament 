public class Solution {
    public int NumberOfMatches(int n) {
        var result = 0;
        while(n > 1){
            result += (n / 2);
            n = n - (n / 2);
        }
        return result;
    }
}